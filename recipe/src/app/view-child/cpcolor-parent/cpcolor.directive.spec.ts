import { CpcolorDirective } from './cpcolor.directive';
import { ElementRef } from '@angular/core';

describe('CpcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new CpcolorDirective();
    expect(directive).toBeTruthy();
  });
});
